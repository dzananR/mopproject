describe ('logging in with instagram account', function(){

    it ('user logs in with instagram account', function(){

        browser.get('https://stg.waroncancer.com/');
        browser.driver.manage().window().maximize();

        element(by.css('[ng-click="vm.showSignUp()"]')).click();

        browser.sleep(3000);

        element(by.buttonText('Sign up with Instagram')).click();

        browser.ignoreSynchronization = true;
        browser.waitForAngular();
        browser.sleep(500); 

        element(by.name('username')).sendKeys('test1¸23');
        browser.sleep(2000);
        element(by.name('password')).sendKeys('trrwztet1');
        browser.sleep(2000);
        element(by.css('.button-green')).click();


    });

});