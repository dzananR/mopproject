describe('login with email', function(){

    it ('to test loging with email', function(){
        browser.get('https://stg.waroncancer.com/');
        browser.driver.manage().window().maximize();

        element(by.css('[ng-click="vm.showSignUp()"]')).click();
        browser.sleep(3000);

        element(by.css('.auth0-lock-input')).sendKeys('hidromehanika6@gmail.com');
        element(by.name('password')).sendKeys('inter1908');

        element(by.name('first_name')).sendKeys('Dzanan');
        element(by.name('last_name')).sendKeys('redzepagic');

        element(by.css('.cl-accept')).click();
        browser.sleep(3000);

        browser.actions().sendKeys(protractor.Key.ENTER).perform();
        browser.sleep(4000);

    });
});